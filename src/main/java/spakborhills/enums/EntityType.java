package spakborhills.enums;

public enum EntityType {
    PLAYER,
    NPC,
    INTERACTIVE_OBJECT, // Contoh: pintu, peti
    PICKUP_ITEM,        // Contoh: kunci, koin
    STATIC_DECORATION   // Contoh: pohon yang tidak bisa ditebang, batu besar
}
